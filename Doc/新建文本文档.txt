绘制逻辑依然不是预期。在进行新绘制时绘制内容会保留到后续帧上并且作为后续帧的绘制内容保存。每帧的绘制内容都需要被保存提交除非这一帧被清屏。比如我在第2帧时绘制了一个a，当我切换到第5帧时将a清屏并绘制了个b并切换到第7帧这时候第七帧原有保存的绘制内容为x，这时在第7帧会显示x，当我切到第8帧时该帧之前没保存过任何绘制内容，则这一帧依然没有任何绘制内容，因为从第7帧开始没有新绘制的内容。在这个情况下提交反馈。再次进行播放或是逐帧播放时希望看到的是第2帧到第4针显示a，第5帧到第6帧显示b第七帧显示x之后的帧什么都没有。帮我比对当前功能差异。