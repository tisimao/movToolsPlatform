# 导演账号功能客户端第一阶段派工指令包

## 1. 阶段目标

本阶段目标是先把导演账号入口、导航、仪表盘与审片任务进入路径收口到正确形态。

目标目录：`D:\project\MovtoolsEx\movtools-client\`

参考文档：

- `D:\project\MovtoolsEx\Doc\导演账号功能设计文档.md`
- `D:\project\MovtoolsEx\Doc\通信协议文档.md`
- `D:\project\MovtoolsEx\Doc\项目固化文档.md`

---

## 2. 本阶段必须完成

1. 导演左侧主导航收口为 `仪表盘 / 镜头 / 审片`
2. 导演登录默认落到导演仪表盘
3. 导演仪表盘展示：我参与的项目、当前激活项目的审片任务、最近反馈记录
4. 仪表盘点击 `开始审片 / 继续审片` 后，可直接进入 `ReviewPage` 并自动选中对应任务
5. 导演镜头页保留为辅助查询入口，不再承担主编辑入口职责

---

## 3. 建议重点排查

- `src/App.tsx`
- `src/auth/permissions.ts`
- `src/pages/DashboardPage.tsx`
- `src/pages/DirectorDashboardPage.tsx`
- `src/pages/ReviewPage.tsx`
- `src/stores/*`

---

## 4. 可直接复制的总派工指令

```md
你现在执行“客户端第一阶段 - 导演入口与主链路收口”任务。

目标目录：
- D:\project\MovtoolsEx\movtools-client\

参考文档：
- D:\project\MovtoolsEx\Doc\导演账号功能设计文档.md
- D:\project\MovtoolsEx\Doc\通信协议文档.md
- D:\project\MovtoolsEx\Doc\项目固化文档.md

必须遵守：
1. 导演主链路必须固定为 `仪表盘 -> 审片任务 -> 审片工作台`
2. 不允许导演继续看到旧的通用项目管理菜单
3. 不允许把导演镜头页当作主编辑入口
4. 不允许只改页面文案，不改真实导航与落点行为

必须完成：
1. 导演左侧主导航收口为 `仪表盘 / 镜头 / 审片`
2. 导演登录默认落到导演仪表盘
3. 导演仪表盘展示：我参与的项目、当前激活项目的审片任务、最近反馈记录
4. 仪表盘点击 `开始审片 / 继续审片` 后，可直接进入 `ReviewPage` 并自动选中对应任务
5. 导演镜头页保留为辅助查询入口，不再承担主编辑入口职责

建议重点排查：
- src/App.tsx
- src/auth/permissions.ts
- src/pages/DashboardPage.tsx
- src/pages/DirectorDashboardPage.tsx
- src/pages/ReviewPage.tsx
- src/stores/*

你必须直接执行：定位 -> 修改 -> 验证 -> 回写说明。

交付时必须输出：
1. 修改 / 新增文件清单
2. 本阶段完成点
3. 与设计文档仍未对齐的剩余点
4. 验证结果
```

---

## 5. 本阶段完成定义

1. 导演主链路已固定为 `仪表盘 -> 审片任务 -> 审片工作台`
2. 导演不会再看到旧的通用项目管理菜单
