# 导演账号功能客户端第二阶段派工指令包

## 1. 阶段目标

本阶段目标是把审片工作台结构、镜头队列、按钮规则和页面布局调到设计文档要求。

目标目录：`D:\project\MovtoolsEx\movtools-client\`

参考文档：

- `D:\project\MovtoolsEx\Doc\导演账号功能设计文档.md`
- `D:\project\MovtoolsEx\Doc\通信协议文档.md`
- `D:\project\MovtoolsEx\Doc\项目固化文档.md`

---

## 2. 本阶段必须完成

1. 工作台采用“上任务镜头列表、中播放器、下反馈区”的纵向结构
2. 镜头队列展示反馈数量、状态与三色逻辑：未匹配红、已匹配待处理黄、已处理绿
3. 播放器底部始终显示进度条
4. `制作人员 / 版本 / 当前帧` 信息卡下移到页面底部或 `测试审片` 上方
5. `标注说明` 文案改为 `反馈说明`
6. 底部右侧列表改为 `草稿与已提交反馈`
7. `保存为草稿`、`提交反馈`、`镜头通过` 规则落地
8. 存在反馈记录时 `镜头通过` 不可点击；反馈列表为 0 时才可点击
9. 移除 `取消镜头返修` 按钮

---

## 3. 建议重点排查

- `src/pages/ReviewPage.tsx`
- `src/components/review/*`
- `src/styles.css`

---

## 4. 可直接复制的总派工指令

```md
你现在执行“客户端第二阶段 - 审片工作台结构与镜头状态交互收口”任务。

目标目录：
- D:\project\MovtoolsEx\movtools-client\

参考文档：
- D:\project\MovtoolsEx\Doc\导演账号功能设计文档.md
- D:\project\MovtoolsEx\Doc\通信协议文档.md
- D:\project\MovtoolsEx\Doc\项目固化文档.md

必须遵守：
1. 工作台主结构必须是“上任务镜头列表、中播放器、下反馈区”
2. 镜头队列颜色逻辑必须是：未匹配红、已匹配待处理黄、已处理绿
3. 存在反馈记录时不得允许 `镜头通过`
4. 不允许保留 `取消镜头返修` 按钮

必须完成：
1. 工作台采用“上任务镜头列表、中播放器、下反馈区”的纵向结构
2. 镜头队列展示反馈数量、状态与三色逻辑：未匹配红、已匹配待处理黄、已处理绿
3. 播放器底部始终显示进度条
4. `制作人员 / 版本 / 当前帧` 信息卡下移到页面底部或 `测试审片` 上方
5. `标注说明` 文案改为 `反馈说明`
6. 底部右侧列表改为 `草稿与已提交反馈`
7. `保存为草稿`、`提交反馈`、`镜头通过` 规则落地
8. 存在反馈记录时 `镜头通过` 不可点击；反馈列表为 0 时才可点击
9. 移除 `取消镜头返修` 按钮

建议重点排查：
- src/pages/ReviewPage.tsx
- src/components/review/*
- src/styles.css

你必须直接执行：定位 -> 修改 -> 验证 -> 回写说明。

交付时必须输出：
1. 修改 / 新增文件清单
2. 本阶段完成点
3. 与设计文档仍未对齐的剩余点
4. 验证结果
```

---

## 5. 本阶段完成定义

1. 页面结构与交互入口已与设计文档一致
2. 未接入绘制前，页面也能正确承载反馈列表与镜头状态交互
