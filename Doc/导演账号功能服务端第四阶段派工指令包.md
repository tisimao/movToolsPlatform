# 导演账号功能服务端第四阶段派工指令包

## 1. 阶段目标

本阶段目标是把所有关键边界做成可验的服务端保证点。

目标目录：`D:\project\MovtoolsEx\movtools-server\`

参考文档：

- `D:\project\MovtoolsEx\Doc\导演账号功能设计文档.md`
- `D:\project\MovtoolsEx\Doc\通信协议文档.md`
- `D:\project\MovtoolsEx\Doc\项目固化文档.md`

---

## 2. 本阶段必须完成

1. 测试覆盖：
   - 只有绘制无文字反馈时提交被拒绝
   - 同一轮多条反馈共享 `feedbackRoundId`
   - 同一轮全部绘制数据成功落库并可回读
   - `CLEAR` 帧可稳定回读
   - 提交反馈后镜头进入 `PENDING_FEEDBACK_FIX`
   - 镜头通过后镜头进入 `DIRECTOR_APPROVED`
   - 提交反馈不改变任务最终结论
2. 任务统计、镜头统计、镜头详情回读在新模型下无冲突
3. 服务端构建与测试通过
4. 对客户端给出当前正式接口组合、字段要求与错误语义说明

---

## 3. 可直接复制的总派工指令

```md
你现在执行“服务端第四阶段 - 校验、回归测试与联调收口”任务。

目标目录：
- D:\project\MovtoolsEx\movtools-server\

参考文档：
- D:\project\MovtoolsEx\Doc\导演账号功能设计文档.md
- D:\project\MovtoolsEx\Doc\通信协议文档.md
- D:\project\MovtoolsEx\Doc\项目固化文档.md

必须遵守：
1. 不允许只有绘制无文字反馈时通过正式提交
2. 不允许 `提交反馈` 改变任务最终结论
3. 不允许镜头详情把不同轮次和未提交本地态混成正式事实

必须完成：
1. 测试覆盖：
   - 只有绘制无文字反馈时提交被拒绝
   - 同一轮多条反馈共享 `feedbackRoundId`
   - 同一轮全部绘制数据成功落库并可回读
   - `CLEAR` 帧可稳定回读
   - 提交反馈后镜头进入 `PENDING_FEEDBACK_FIX`
   - 镜头通过后镜头进入 `DIRECTOR_APPROVED`
   - 提交反馈不改变任务最终结论
2. 任务统计、镜头统计、镜头详情回读在新模型下无冲突
3. 服务端构建与测试通过
4. 对客户端给出当前正式接口组合、字段要求与错误语义说明

你必须直接执行：定位 -> 修改 -> 测试 -> 回写说明。

交付时必须输出：
1. 修改 / 新增文件清单
2. 本阶段完成点
3. 接口/字段/状态语义变化说明
4. 验证结果
5. 剩余风险
```

---

## 4. 本阶段完成定义

1. 服务端已具备可长期维护的导演反馈轮次与遮罩模型
2. 与客户端联调所需的正式保证点已齐备
